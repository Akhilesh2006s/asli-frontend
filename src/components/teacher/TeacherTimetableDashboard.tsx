import TimetablePhotoManagement from '@/components/admin/TimetablePhotoManagement';

/** Teacher uploads/views class timetable photos (same flow as admin, scoped classes). */
export default function TeacherTimetableDashboard() {
  return <TimetablePhotoManagement forTeacher />;
}
